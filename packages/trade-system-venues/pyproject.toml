[project]
name = "trade-system-venues"
version = "0.1.1"
description = "NautilusTrader venue fee and financing models (Binance, IBKR)"
authors = [{name = "shawndeng", email = "shawndeng1109@qq.com"}]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12,<3.14.99"
dependencies = [
    "nautilus-trader[ib]~=1.230.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trade_system_venues"]
